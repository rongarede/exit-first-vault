{
  "name": "@exit-first/contracts",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "forge build",
    "test": "forge test -vv",
    "test:fork": "forge test -vv --fork-url $BASE_RPC",
    "clean": "forge clean"
  }
}
